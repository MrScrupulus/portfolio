services:
  frontend:
    build: ./frontend
    ports:
      - "3036:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    # Pas exposé sur l'hôte : nginx y accède via le réseau Docker (port 8000)
